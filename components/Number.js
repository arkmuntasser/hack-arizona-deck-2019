import React from 'react'

export default ({ children }) => (
    <div
        style={{
            fontSize: '136px',
            fontWeight: '700',
            textAlign: 'center'
        }}
    >{children}</div>
)