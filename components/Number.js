import React from 'react'

export default ({ children }) => (
    <span
        style={{
            display: 'block',
            fontSize: '136px',
            fontWeight: '700',
            textAlign: 'center'
        }}
    >{children}</span>
)